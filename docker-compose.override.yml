services:
  ollama:
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]

  portal-api:
    environment:
      - PORTAL_ENV=development
      - PORTAL_BOOTSTRAP_API_KEY=${PORTAL_BOOTSTRAP_API_KEY:-portal-dev-key}
